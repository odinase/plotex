function init(obj)
    obj.amount_of_data = length(obj.data);
    obj.font_size = new_font_size();
    obj.disable_figure = false;
    obj.use_loglog = false;
    obj.use_grid_on = false;
    obj.use_thick_lines = false;
end

